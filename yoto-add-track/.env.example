# Yoto developer Client ID — register at https://yoto.dev/get-started/start-here/
YOTO_CLIENT_ID=your_client_id_here

# Path to the OAuth token file (created on first run).
# Default: token.json next to the script.
# TOKEN_FILE=token.json
